import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <React.StrictMode>
    <div>
      <h1>hello</h1>
    </div>
  </React.StrictMode>,
  document.getElementById("root")
);
